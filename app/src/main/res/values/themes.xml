<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NOBERRY" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:windowBackground">@color/bg</item>
        <item name="android:fontFamily">@font/firasanscondensed_regular</item>
        <item name="fontFamily">@font/firasanscondensed_regular</item>
        <!-- target android sdk versions < 26 and > 14 if theme other than AppCompat -->
    </style>

    <style name="Theme.NOBERRY" parent="Base.Theme.NOBERRY" />
</resources>